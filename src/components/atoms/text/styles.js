import {StyleSheet} from 'react-native';
import colors from '../../../assets/colors';

export default StyleSheet.create({
    text: {
        color: colors.black,
        fontSize: 15,
        fontWeight: '400',
        fontFamily: "System"
    }
});
